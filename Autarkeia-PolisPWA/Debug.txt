Recharts has a known issue where calculating dimensions in certain absolute flex containers (where initial sizes might report as 0 or calculated flex boundaries report as -1 during render cycles) produces the negative dimension warning. I've switched the ResponsiveContainer dimension strategy to use width="99%" (a standard layout fix that accommodates browser resize calculation rounding limitations) and set a strict height={220} rather than height="100%" to avoid recursive height collapse during mount.